import React from 'react';
import { Container, Card } from 'react-bootstrap';

const UserInfo = () => {
  return (
    <Container>
      <Card>This is will be a chat box</Card>
    </Container>
  );
};

export default UserInfo;
